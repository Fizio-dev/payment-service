import type { Disposable } from 'tsyringe';
import { type MQPublisherOpts } from './config.js';
export declare class MQPublisher implements Disposable {
    private readonly opts;
    private readonly logger;
    private readonly connection;
    private readonly channelManager;
    private initialized;
    constructor(opts: MQPublisherOpts);
    initialize(): Promise<void>;
    send(queueName: string, message: unknown): Promise<void>;
    sendBatch(queueName: string, messages: unknown[]): Promise<void>;
    dispose(): Promise<void>;
}
//# sourceMappingURL=mq-publisher.d.ts.map