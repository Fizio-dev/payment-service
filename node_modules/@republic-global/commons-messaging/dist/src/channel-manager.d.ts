import type { AmqpConnectionManager, ChannelWrapper } from 'amqp-connection-manager';
import type { ChannelOpts } from './config.js';
export type ChannelType = 'publish' | 'subscribe';
export declare class ChannelManager {
    private readonly connection;
    private readonly opts;
    private readonly channelType;
    private readonly logger;
    private readonly channels;
    private initialized;
    constructor(connection: AmqpConnectionManager, opts: ChannelOpts, channelType: ChannelType);
    initialize(): Promise<void>;
    pickChannel(): ChannelWrapper;
    close(): Promise<void>;
}
//# sourceMappingURL=channel-manager.d.ts.map