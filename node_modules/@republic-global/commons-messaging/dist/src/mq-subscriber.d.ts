import type { Disposable } from 'tsyringe';
import { type MQSubscriberOpts } from './config.js';
export declare class MQSubscriber implements Disposable {
    private readonly opts;
    private readonly logger;
    private readonly connection;
    private readonly channelManager;
    private initialized;
    constructor(opts: MQSubscriberOpts);
    initialize(): Promise<void>;
    registerSubscriber(queueName: string, messageConsumer: (message: unknown) => Promise<void>): Promise<void>;
    dispose(): Promise<void>;
    private handleError;
}
//# sourceMappingURL=mq-subscriber.d.ts.map