import { CacheService } from './cache.service.js';
export declare class PresenceService {
    private readonly cacheService;
    constructor(cacheService: CacheService);
    setOnline(ccId: string, socketId: string): Promise<void>;
    setOffline(ccId: string, socketId: string): Promise<void>;
    areUsersOnline(ccIds: string[]): Promise<Record<string, boolean>>;
    private isNumber;
}
//# sourceMappingURL=presence.service.d.ts.map