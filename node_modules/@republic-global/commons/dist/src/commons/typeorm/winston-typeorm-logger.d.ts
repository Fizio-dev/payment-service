import { AbstractLogger, type LogLevel, type LogMessage, type LoggerOptions, type QueryRunner } from 'typeorm';
import type { Logger } from 'winston';
/**
 * Custom typeorm logger based on winston logger.
 */
export declare class WinstonTypeOrmLogger extends AbstractLogger {
    private readonly winstonLogger;
    constructor(winstonLogger: Logger, options?: LoggerOptions);
    /**
     * Write log to specific output.
     */
    protected writeLog(level: LogLevel, logMessage: LogMessage | LogMessage[], queryRunner?: QueryRunner): void;
    private static buildMeta;
}
//# sourceMappingURL=winston-typeorm-logger.d.ts.map