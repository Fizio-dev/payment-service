import { Passport } from 'passport';
import { crowdCraftJwtStrategy, crowdcraftAnonymousStrategy, } from '../strategy/index.js';
const passport = new Passport();
passport.use(crowdCraftJwtStrategy);
passport.use(crowdcraftAnonymousStrategy);
export const jwtAuthAnonymous = (req, res, next) => {
    passport.authenticate(['jwt', 'anon'], { session: false })(req, res, next);
};
//# sourceMappingURL=jwt-auth-anonymous.js.map